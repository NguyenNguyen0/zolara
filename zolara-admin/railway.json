{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "runtime": "V2",
        "startCommand": "npm run build && PORT=$PORT npm run preview",
        "numReplicas": 1,
        "healthcheckPath": "/"
    }
}